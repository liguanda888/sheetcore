// SheetCore 模块根包。
//
// 这里只放**模块级元信息**（版本号、一句话描述），不放任何计算逻辑 ——
// 逻辑都在子包里，每个子包有各自的测试。
//
// 为什么要有这个包：没有它，根目录下的 `sheetcore.mbt` 不参与编译，
// 也不参与检查。它曾经就是这样躺了两个版本 —— 里面的子包清单漏了
// `render/` 和 `cli/`，注释里还留着一个早已不存在的 `serverInfo`，
// 而且谁也发现不了，因为它从不被编译。

import {
}

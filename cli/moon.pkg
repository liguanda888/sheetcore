// cli —— 命令行解析（纯逻辑，可测试）
//
// 为什么单独成包：这些规则原来长在 `cmd/main` 里，而那是唯一没有测试的包。
// 于是这条缺陷只能靠手敲才发现：
//
//     $ sheetcore nosuchcmd
//     error: expected CELL=CONTENT but got: nosuchcmd
//
// 拼错的子命令落到了「裸写单元格」的分支里，报出答非所问的错。
// 同一层的其它毛病也都躲过了测试：`bench --help` 报错而不是给帮助、
// `demo 多余参数` 被静默忽略、`--length=0` 的报错却把三个选项都念一遍。
//
// 规则搬进来之后，每条分支都能被断言钉住；`cmd/main` 退化成纯粹的 I/O。

import {
}

// SheetCore —— 模块配置
// 文档：https://docs.moonbitlang.com/en/latest/toolchain/moon/module.html

name = "liguanda888/sheetcore"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/liguanda888/sheetcore"

license = "Apache-2.0"

keywords = [
  "spreadsheet",
  "formula",
  "parser",
  "dependency-graph",
  "incremental",
]

description = "A spreadsheet calculation core in pure MoonBit: formula language, dependency graph, incremental recalculation and error propagation"

preferred_target = "native"

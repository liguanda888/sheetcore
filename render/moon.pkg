// render —— 把值排成人类可读的文本（纯逻辑，可测试）
//
// 为什么单独成包：**列宽计算是纯逻辑，而且很容易写错**。
// 第一版放在 `cmd/main` 里，用「字符数」补空格，于是中英文混排时整张表歪掉 ——
// 因为一个汉字占 **2 个显示列**而只占 1 个字符。CLI 层没有测试，这个 bug
// 只能靠肉眼看截图发现。
//
// 放进这里之后，宽度规则可以被断言钉住。

import {
}
